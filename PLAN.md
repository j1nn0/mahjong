# リーチ麻雀 東風戦ルール拡張計画

## 目的

東風戦だけを対象に、一般的なリーチ麻雀のルールを段階的に追加する。半荘、ウマ・オカ、責任払い、チョンボは今回の対象外とする。

## 合意済みルール方針

- 東風戦のみを対象にする
- ルール名は `リーチ麻雀` を正語にする
- アリアリを採用する
- チョンボは扱わず、不正操作はできるだけ操作不可として扱う
- ウマ・オカは扱わず、順位は素点で決める
- 責任払いは今回扱わない

## 実装済み（Phase 1〜9）

- [x] 用語整理・ADR・README 整備
- [x] 食い替え禁止
- [x] 途中流局: 九種九牌、四風連打、四家立直、四槓散了
- [x] 複数ロン: ダブロン、三家和
- [x] 流し満貫
- [x] トビ終了
- [x] totalHan 食い下がりバグ修正
- [x] 天和・地和の追加

最新の検証結果（Phase 9 完了時点）:

- `rtk pnpm test`: 10 files / 155 tests passed
- `rtk pnpm exec tsc --noEmit`: exit 0

---

## 次の作業

改善の優先順は「バグ・ルール上の問題 → AI改善 → UX改善 → コード品質」の順とする。

---

### Phase 10: 役満時のスコア表示修正（UIバグ）

`ScoreResult.han` と `ScoreResult.fu` が役満時は `0` のため、UI に `飜: 0 (役) + 0 (ドラ) = 0` と表示されるバグを修正する。

受け入れ条件:

- [x] `App.tsx` のスコア表示部分で `sr.yakuman > 0` の場合は翻/符の行を非表示にする
- [x] 代わりに「役満」または「ダブル役満」の区分と倍数（×1 / ×2）を表示する
- [x] 役一覧（役名）は従来通り表示する
- [x] 天和・地和のスコア画面も同様の表示になっていることを確認する

想定変更ファイル:

- `src/ui/App.tsx`

---

### Phase 11: チーの複数候補選択

チー時に手牌から複数の組み合わせが存在する場合（例: 3・4・5があり5を切った他家に対して3-4か4-5でチーできる）、現在は先頭候補を固定で選んでしまう問題を修正する。

受け入れ条件:

- [x] チーの候補が2件以上ある場合、`←→` キーで候補を切り替えられるようにする
- [x] `ClaimMenu` コンポーネントがチー候補を個別に表示する
- [x] `C` キー押下で現在選択中のチー候補が確定される
- [x] AI はチー候補が複数あっても問題なく動作する（現状維持）
- [x] 回帰テストを追加する

想定変更ファイル:

- `src/ui/App.tsx`
- `src/state/GameState.test.ts`

---

### Phase 12: AIのカン宣言

現在の AI は暗槓・加槓を宣言しない。役満（四槓子）や点数効率の改善のため、AI にカン宣言ロジックを追加する。

受け入れ条件:

- [x] `processAiTurn` で AI のターン時に、暗槓できる牌があれば暗槓を宣言する
- [x] 加槓できる牌があれば加槓を宣言する
- [x] リーチ中の AI は暗槓可能でも（手が変わる暗槓のみ禁止なので）暗槓を宣言しない
- [x] カン後のリンシャン牌でツモ和了できる場合はツモを宣言する
- [x] AI のカン宣言に関するテストを追加する

想定変更ファイル:

- `src/state/GameState.ts`（`processAiTurn`）
- `src/state/GameState.test.ts`

---

### Phase 13: テンパイ表示

リーチをかけていない状態でテンパイしている場合、プレイヤーに分かるよう表示を追加する。

受け入れ条件:

- [x] 自分のターン（手牌14枚）でテンパイしている場合、`[T]ツモ` ボタンの横に待ち牌の種類数を表示する（例: `待ち: 8種`）
- [x] 待ち牌を具体的に表示するオプションをアクションバーに追加する（例: `2m 5m 8m`）
- [x] リーチ中は表示しない（情報が重複するため）

想定変更ファイル:

- `src/ui/App.tsx`

---

### Phase 14: AIの危険牌評価改善

現在の `evaluateDanger` は現物・スジのみの判定。より実戦的な評価に改善する。

受け入れ条件:

- [ ] 字牌（風牌・三元牌）について、枚数が多く場に出ていない牌は危険度を上げる
- [ ] リーチ者の捨て牌に数牌の両端が多い場合、中張牌の危険度を上げる
- [ ] リーチ者でない相手についても、副露の構成から待ちを推測して危険度評価に反映する
- [ ] AI の打牌安全度評価に関するテストを追加する

想定変更ファイル:

- `src/game/ai.ts`
- `src/game/ai.test.ts`

---

### Phase 15: カンドラの公開タイミング修正

現在は暗槓・加槓直後に `doraCount` をインクリメントしているが、正しくは槓後の打牌が終わった時点（搶槓ロンが確定した後）でカンドラを公開する。

受け入れ条件:

- [x] カンドラの公開タイミングを正しく管理する。
  - [x] 暗槓の場合、カンドラは「即座に（打牌前に）」公開する
  - [x] 明槓・加槓の場合、カンドラは「打牌後に」公開する
  - [x] 既存のロジックがこれと異なる場合、ルールに従うように修正する

想定変更ファイル:

- `src/state/GameState.ts`
- `src/state/GameState.test.ts`

---

### Phase 16: フリテン検証の確認と修正

ロン時の `temporaryFuriten`（同巡フリテン）と `riichiFuriten`（リーチフリテン）が実際にロン拒否として機能しているかコードレビューし、不足があれば修正する。

受け入れ条件:

- [x] ロン候補生成時（`canScoreRon` または相当ロジック）で `temporaryFuriten` が `true` のプレイヤーをロン候補から除外していることを確認する
- [x] リーチ中に自分の待ち牌を見逃した場合に `riichiFuriten` が `true` になることを確認する
- [x] フリテンのロン拒否に関するテストを追加する（テンパイしているがフリテン状態でロンできないシナリオ）
- [x] リーチ宣言牌をロンされなかった場合の本人のフリテン処理が正しいことを確認する

想定変更ファイル:

- `src/state/GameState.ts`
- `src/state/GameState.test.ts`

---

### Phase 17: 最終結果画面の改善

対戦終了後の結果表示を充実させる。

受け入れ条件:

- [x] 最終結果画面に各局の結果サマリーを表示する（局名・和了者・役・収支）
- [x] 局履歴を `GameState` に蓄積する（`roundHistory` フィールドを追加）
- [x] 最終順位の並び順を素点降順で表示する（現在は固定順）
- [x] 対戦終了後に「もう一度」と「終了」の2択を表示する

想定変更ファイル:

- `src/state/GameState.ts`
- `src/ui/App.tsx`
- `src/state/GameState.test.ts`

---

### Phase 18: 天和処理の重複排除（リファクタリング）

`dealRound` 内に天和の点数計算・支払い処理が独立して記述されており、通常の `TSUMO` 処理と重複している。`fullScore` → `finishRound` の流れに統一する。

受け入れ条件:

- [x] `dealRound` 内の天和処理を、`TSUMO` アクションと共通の `applyTsumoPayment` 関数に切り出す
- [x] 切り出し後もすべての既存テストがパスする
- [x] 処理の重複が解消されていることをコードレビューで確認する

想定変更ファイル:

- `src/state/GameState.ts`

---

### Phase 19: processAiTurn のテスト追加

AIの挙動（カン宣言・リーチ後のツモ切り・危険牌回避）に関するテストが不足している。

受け入れ条件:

- [ ] AI がリーチ後にツモ切りを行うシナリオをテストする
- [ ] AI がリーチ者に対して現物を選ぶシナリオをテストする
- [ ] AI がカン宣言するシナリオをテストする（Phase 12 と連動）
- [ ] AI が九種九牌を自動宣言するシナリオをテストする

想定変更ファイル:

- `src/state/GameState.test.ts`
- `src/game/ai.test.ts`

---

### Phase 20: 最終検証

- [ ] `rtk pnpm test`
- [ ] `rtk pnpm exec tsc --noEmit`
- [ ] README と `CONTEXT.md` のスコープが実装状態と一致している
- [ ] `PLAN.md` の進捗を更新する
