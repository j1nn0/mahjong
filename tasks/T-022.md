---
id: T-022
title: AIロジックの改善（自己危険度評価・スジ判定の修正）
status: done
assignee: null
priority: high
tags:
  - ai,gameplay
depends_on: []
created_at: 2026-06-22
updated_at: 2026-06-22
---

## Goal
AIが切る牌を決定する際のロジックを改善します。
1. `evaluateDanger` で手番プレイヤー（自分自身）の情報も「対戦相手」として巻き込んで評価してしまうため、自分がリーチしていると手牌の全牌が危険判定されて安全牌選択が壊れる問題を修正します。
2. 切られた牌に対して、そのスジグループ全体を一括してスジ（安全）と判定してしまうため、本来安全ではない牌（片スジなど）をスジと判定してしまう不正確なスジ判定ロジックを修正します。

## Acceptance Criteria
- [x] `aiChooseDiscard` または `evaluateDanger` の中で、手番プレイヤー（自分自身）のデータを危険度評価のループから除外する。
- [x] 自分がリーチしたことによって、手牌の全ての牌の危険度が `8 (DANGER_HIGH)` などに不当に上がらないことを検証するテストケースを追加する。
- [x] スジ判定ロジック（`sujiIndices` や `discardSuji` の使われ方）を、麻雀の本来の安全牌ルール（中スジ、端牌のスジなど）に基づいて正しく処理するように修正する。
  - `1` が切られていても `4` や `7` はスジと見なさない（`4-7` の両面待ちがあるため）。
  - `4` が切られているとき、`1` と `7` はスジと見なす。
  - `1` と `7` の両方が切られているときのみ、`4` を中スジと見なす。
- [x] 正しいスジ判定を検証するユニットテストを追加する。
- [x] `pnpm test` がすべてパスすること。

## Notes
- `src/game/ai.ts` の `evaluateDanger` や `sujiIndices` 辺りが修正対象となります。

## Progress
- 2026-06-22: 実装完了。
  - `evaluateDanger` / `evaluateDiscards` / `aiChooseDiscard` に `selfIndex?: number` パラメータを追加し、危険度評価ループから自分自身を除外するように修正。
  - `GameState.ts` の `processAiTurn` 内の `aiChooseDiscard` 呼び出し2箇所に `state.currentPlayer` を渡すよう修正。
  - `sujiMap` / `sujiIndices` を削除し、正しいスジ理論に基づく `computeSujiTiles` 関数に置き換え（片スジ: 4/5/6が切られた→端の牌が安全、中スジ: 両端の牌が両方切られた→中央の牌が安全）。
  - `dangerForPlayer` のリーチ時スジ判定を `computeSujiTiles` を使うよう修正。
  - テスト追加: スジ判定6ケース + 自己除外2ケース。既存テストも全てパス。
  - レビュー指摘を反映し、`selfIndex` は対戦相手ごとの危険度評価だけから除外し、字牌の `visibleCount` には自分の捨て牌・副露牌も含めるよう修正。回帰テストを追加。
  - `pnpm test`: 287 passed / 1 skipped。`tsc --noEmit`: エラーなし。
